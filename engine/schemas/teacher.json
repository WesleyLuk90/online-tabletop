{
    "title": "Example Schema",
    "type": "object",
    "properties": {
        "firstName": {
            "type": "string"
        },
        "lastName": {
            "type": "string"
        },
        "value": {
            "$ref": "#/definitions/baz"
        }
    },
    "definitions": {
        "baz": {
            "type": "string",
            "title": "NamedInteger",
            "tsEnumNames": ["red", "amber", "green"],
            "enum": ["red", "amber", "green"]
        }
    },
    "additionalProperties": false,
    "required": ["firstName", "lastName"]
}
