{
    "private": true,
    "workspaces": [
        "client",
        "server",
        "engine"
    ],
    "scripts": {
        "run-services": "docker-compose -f docker-compose.services.yml up -d",
        "dev": "yarn workspace server dev",
        "build": "tsc"
    },
    "devDependencies": {
        "typescript": "~4.0.2"
    }
}
