language: node_js
node_js:
    - "8"

script:
    - yarn install --frozen-lockfile
    - cd protocol
    - yarn build
    - yarn test
    - cd ../server
    - yarn build
    - yarn test
    - cd ../client
    - yarn build
    - yarn test
